import 'package:flutter/material.dart';

class AppColors {
  // Tema 1
  static const Color lightPrimary = Colors.deepPurple;
  static const Color lightBackground = Colors.white;

  // Tema 2
  static const Color darkPrimary = Colors.tealAccent;
  static const Color darkBackground = Colors.black;

  // Tema 3 (Retro)
  static const Color retroPrimary = Color(0xFFE91E63);
  static const Color retroBackground = Color(0xFFFFF8E1);
}
